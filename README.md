# AI Web Article Generator

一个基于人工智能的Web文章生成器，使用大型语言模型(LLM)技术帮助用户创建高质量的文章内容。

## 项目简介

AI Web Article Generator 是一个全栈Web应用，包含前端界面和后端服务，旨在利用先进的AI技术帮助用户快速生成高质量的文章内容。无论是博客文章、新闻报道还是创意写作，本应用都能提供强大的辅助功能。

## 技术栈

### 后端 (node_admin)
- **框架**: Express.js
- **语言**: Node.js
- **AI集成**: OpenAI API, LangChain
- **数据库**: ChromaDB (向量数据库)
- **认证**: JWT (JSON Web Tokens)
- **文件处理**: Multer
- **日志**: Winston

### 前端 (vue_web)
- **框架**: Vue.js 3
- **构建工具**: Vite
- **UI组件**: Element Plus
- **状态管理**: Pinia
- **路由**: Vue Router
- **编辑器**: Monaco Editor
- **HTTP客户端**: Axios

## 功能特点

- 🤖 AI驱动的内容生成
- 📝 富文本编辑器
- 📚 文章管理和存储
- 👤 用户认证和授权
- 🔄 实时预览和编辑
- 📊 使用统计和分析
- 🌐 响应式设计

## 安装与运行

### 环境要求

- Node.js >= 16.0.0
- npm 或 yarn

### 后端设置

1. 进入后端目录:
   ```bash
   cd node_admin
   ```

2. 安装依赖:
   ```bash
   npm install
   ```

3. 创建环境变量文件 `.env` 并配置必要变量:
   ```
   OPENAI_API_KEY=your_openai_api_key
   PORT=3000
   JWT_SECRET=your_jwt_secret
   ```

4. 启动开发服务器:
   ```bash
   npm run dev
   ```

### 前端设置

1. 进入前端目录:
   ```bash
   cd vue_web
   ```

2. 安装依赖:
   ```bash
   npm install
   ```

3. 启动开发服务器:
   ```bash
   npm run dev
   ```

### 生产环境部署

1. 构建前端:
   ```bash
   cd vue_web
   npm run build
   ```

2. 启动后端生产服务:
   ```bash
   cd node_admin
   npm start
   ```

## 项目结构

```
aiWebArticleGenerator/
├── node_admin/          # 后端服务
│   ├── app.js           # 应用入口
│   ├── routes/          # 路由定义
│   ├── services/        # 业务逻辑
│   ├── utils/           # 工具函数
│   ├── data/            # 数据存储
│   └── uploads/         # 上传文件
└── vue_web/             # 前端应用
    ├── src/
    │   ├── components/  # Vue组件
    │   ├── views/       # 页面视图
    │   ├── router/      # 路由配置
    │   ├── store/       # 状态管理
    │   └── utils/       # 工具函数
    ├── public/          # 静态资源
    └── dist/            # 构建输出
```

## 贡献指南

我们欢迎任何形式的贡献！如果您想参与项目开发，请遵循以下步骤：

1. Fork 本仓库
2. 创建您的特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交您的更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启一个 Pull Request

## 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。

## 联系我们

- 项目主页: [GitHub Repository](https://github.com/your-username/aiWebArticleGenerator)
- 问题反馈: [Issues](https://github.com/your-username/aiWebArticleGenerator/issues)

## 致谢

感谢所有为这个项目做出贡献的开发者！
